spring:
  profiles:
    active: dev
  application:
    name: simple-storage-registry-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/